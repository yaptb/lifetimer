<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LifeTimer.ReleaseNotesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LifeTimer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ReleaseNotesWIndow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <RichEditBox 
        x:Name="ReleaseNotesViewer" 
        Grid.Row="0"
        VerticalAlignment="Stretch" 
        HorizontalAlignment="Stretch"
        Loaded="ReleaseNotesViewer_Loaded"
        Margin="24"
        Background="#FF333333"
        Foreground="White"
        >

            <RichEditBox.Resources>


                <SolidColorBrush x:Key="TextControlBackground" Color="#FF333333"/>
                <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="#FF333333"/>
                <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="#FF333333"/>

                <SolidColorBrush x:Key="TextControlForeground" Color="White"/>
                <SolidColorBrush x:Key="TextControlForegroundPointerOver" Color="White"/>
                <SolidColorBrush x:Key="TextControlForegroundFocused" Color="White"/>
            </RichEditBox.Resources>

        </RichEditBox>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="24">
            <Button x:Name="Close" Click="Close_Click">Close</Button>
        </StackPanel>

    </Grid>
</Window>
