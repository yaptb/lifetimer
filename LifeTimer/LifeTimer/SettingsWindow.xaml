<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LifeTimer.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LifeTimer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    xmlns:tb="using:H.NotifyIcon"
    xmlns:usercontrols="using:LifeTimer.Controls"
    xmlns:settingscontrols="using:LifeTimer.Controls.Settings"
    xmlns:layoutcontrols="using:LifeTimer.Controls.Layout"
    >

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    
    <Grid >

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="center" HorizontalAlignment="Stretch" Margin="0,16,16,16">

            <!-- define our navigation items. Events are wired up in code behind-->
            <layoutcontrols:SidenavItemControl x:Name="CommandsItem" x:Uid="SettingsWindow_CommandItem"></layoutcontrols:SidenavItemControl>
            <layoutcontrols:SidenavItemControl x:Name="TimersItem" x:Uid="SettingsWindow_TimersItem" ></layoutcontrols:SidenavItemControl>
            <layoutcontrols:SidenavItemControl x:Name="AppearanceItem" x:Uid="SettingsWindow_AppearanceItem" ></layoutcontrols:SidenavItemControl>
            <layoutcontrols:SidenavItemControl x:Name="SettingsItem" x:Uid="SettingsWindow_SettingsItem"  ></layoutcontrols:SidenavItemControl>
            <layoutcontrols:SidenavItemControl x:Name="HelpItem"     x:Uid="SettingsWindow_HelpItem"  ></layoutcontrols:SidenavItemControl>

          
        </StackPanel>

 
        <!--sidenav tab content -->
        <Grid 
            Grid.Row="0"
            Grid.Column="1"
         >
            
            <!--define the navigation tabs. items are mapped to tabs in the code behind -->
            <usercontrols:CommandTabUserControl x:Name="CommandsTab"></usercontrols:CommandTabUserControl>

            <usercontrols:TimerTabUserControl x:Name="TimersTab"></usercontrols:TimerTabUserControl>

            <usercontrols:AppearanceTabUserControl x:Name="AppearanceTab"></usercontrols:AppearanceTabUserControl>

            <usercontrols:SettingsTabUserControl x:Name="SettingsTab"></usercontrols:SettingsTabUserControl>
            <!--<usercontrols:HelpTabUserControl x:Name="HelpTab"></usercontrols:HelpTabUserControl> -->
            <usercontrols:DummyUserControl x:Name="HelpTab"></usercontrols:DummyUserControl> 

        </Grid>

        
    <settingscontrols:SettingsStatusControl x:Name="SettingsStatus" Grid.Row="1" Grid.ColumnSpan="2" ></settingscontrols:SettingsStatusControl>


    </Grid>

</Window>
