<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LifeTimer.Controls.Buttons.EditBarButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LifeTimer.Controls.Buttons"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      x:Name="This"
    mc:Ignorable="d">

    <Grid>

        <!--
        <Grid.RenderTransform>
            <ScaleTransform ScaleX="{x:Bind ButtonScale, Mode=OneWay}"
                            ScaleY="{x:Bind ButtonScale, Mode=OneWay}" />
        </Grid.RenderTransform>
        -->

        <Grid.Resources>
            <x:Double x:Key="AppBarIconSize">15</x:Double>
        </Grid.Resources>

        <Button x:Name="PART_Button"
                Background="Transparent"
                BorderThickness="0"
                BorderBrush="Aqua"
                Click="PART_Button_Click"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                   IsTabStop="False"
                AllowFocusOnInteraction="False"
                Foreground="Wheat"
                Width="20"
                Height="20">

            <!--  
            <Button.Resources>
                <StaticResource x:Key="ButtonForeground" ResourceKey="AccentFillColorDefaultBrush"/>
                <StaticResource x:Key="ButtonForegroundPointerOver" ResourceKey="AccentFillColorDefaultBrush"/>
            </Button.Resources>
-->
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid x:Name="RootGrid"
                          Background="{TemplateBinding Background}"
                         >

                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">


                            <FontIcon
                                 Foreground="{StaticResource AccentFillColorDefaultBrush}"
                                 Glyph="{Binding Glyph, ElementName=This}"
                                 FontSize="12"
                                
                                       ></FontIcon>
                        </StackPanel>

                        <!--
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <FontIcon Glyph="{Binding Glyph, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                      FontFamily="Segoe MDL2 Assets"
                                      FontSize="{StaticResource AppBarIconSize}"
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                        -->

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                                                                       Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0"
                                                Value="{ThemeResource SystemControlHighlightListLowBrush}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                                                                       Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0"
                                                Value="{ThemeResource SystemControlHighlightListMediumBrush}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                                                                       Storyboard.TargetProperty="Opacity">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="0.4"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>


                    </Grid>
                </ControlTemplate>
            </Button.Template>
        </Button>

    </Grid>

</UserControl>
